# Copy to docker/.env and edit.  Used by docker-compose.yml.
#
# Absolute path to your Volt Active Data Enterprise trial license (mounted read-only).
LICENSE_FILE=/absolute/path/to/license.xml

# Load-generator defaults for `docker compose run --rm loadgen`.
EPS=2000
CUSTOMERS=10000000
